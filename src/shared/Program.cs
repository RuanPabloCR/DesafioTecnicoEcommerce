namespace shared
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            // This is a shared project, it does not run by itself.
            // Its purpose is to share code between other projects.
            // You can add shared code here if needed.
        }
    }
}
